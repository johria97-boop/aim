# Base URL of the A.I.M. backend API (no trailing slash)
# For local development:
VITE_API_BASE_URL=http://localhost:5000
# For production, set this to your deployed backend URL in Vercel Environment Variables
